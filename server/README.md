Sign in at [https://engine.apollographql.com/](https://engine.apollographql.com/),
get api key and update `APOLLO_ENGINE_API_KEY` constant in `src/index.js` file.

Then just

```bash
npm install
npm run start
```
